const fs = require('fs'); const s = JSON.parse(fs.readFileSync('swagger.json', 'utf8')); const adminApis = []; Object.entries(s.paths).forEach(([path, methods]) => { Object.entries(methods).forEach(([method, details]) => { if (details.summary && details.summary.toLowerCase().includes('admin')) { adminApis.push(method.toUpperCase() + ' ' + path + ' - ' + details.summary); } }); }); console.log(adminApis.join('\n'));
